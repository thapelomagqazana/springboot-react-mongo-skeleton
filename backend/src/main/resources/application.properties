spring.application.name=backend

# MongoDB connection
spring.data.mongodb.uri=mongodb://localhost:27017/skeletondb

# JWT secret key (hardcoded here, use environment variable in production)
app.jwt.secret=super_secret_jwt_key

# Server port
server.port=8080

